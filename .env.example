# Crimen Environment Configuration
# Copy this to .env and fill in values. Never commit .env to source control.

NODE_ENV=development

# Server
PORT=9000
IP=0.0.0.0

# MongoDB
MONGODB_URI=mongodb://localhost/crimespace
